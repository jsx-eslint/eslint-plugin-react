'use strict';

const react = require('eslint-plugin-react');

/** @type {import('eslint').Linter.Config[]} */
const config = [
  {
    plugins: {
      react,
    },
  },
];

module.exports = config;
